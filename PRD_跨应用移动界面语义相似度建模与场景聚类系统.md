# PRD：面向跨应用的移动界面场景识别、相似检索与原型聚类系统

## 1. 项目概述

### 1.1 项目名称
面向跨应用的移动界面场景识别、相似检索与原型聚类系统

### 1.2 项目定位
本项目面向移动界面理解这一具体问题，利用现有的截图与 UI 树 JSON 配对数据，构建一个轻量、可复现、适合个人大创推进的多模态系统。系统先完成页面场景识别，再在统一语义空间中实现相似页面检索和场景原型聚类，为后续跨应用自动化、智能体感知和页面理解提供基础能力。

### 1.3 一句话描述
让模型不仅知道“这是什么页面”，还知道“这个页面和哪些页面本质上属于同一种使用场景”。

## 2. 立项目标与现实场景

### 2.1 研究背景
不同 App 的页面设计风格差异很大，但在用户任务层面存在大量稳定的共性场景，例如登录注册、商品详情、商品列表、聊天消息、支付确认、视频播放等。传统方法往往依赖单 App 规则，迁移性差；而直接依赖大模型在线理解又存在成本高、响应慢、难复现的问题。

对个人大创项目而言，更合适的路线不是一开始追求特别重的端侧部署或复杂智能体系统，而是先构建一个可验证的“界面语义中间层”：输入截图与 UI 树，输出页面类别、语义向量、相似页面和聚类归属。

### 2.2 实际应用场景
本项目聚焦以下三个真实且可展示的场景：

- 场景识别：给定一张手机页面截图和对应 UI 树，判断该页面属于登录、商品详情、视频播放等哪一类场景。
- 相似页面检索：给定一个页面，返回最相似的 Top-K 页面，用于验证模型是否学到了跨应用场景共性。
- 场景原型聚类：把大量页面自动聚成若干簇，并为每个簇挑出代表页面，形成“原型页面库”。

### 2.3 本项目适合大创的原因

- 数据已经具备：现有 `data_qwen_split` 已按 `train/val/test` 划分完成。
- 输入已经配对：每个样本同时包含截图和 UI 树 JSON，可直接做多模态。
- 技术路线可分阶段推进：先分类，后检索，再聚类，便于逐步出结果。
- 个人可实现：不依赖超大模型在线推理，不依赖复杂移动端工程。

## 3. 当前数据基础

### 3.1 数据来源
当前主数据集为 [data_qwen_split](d:\HuaweiMoveData\Users\曾景麟\Desktop\apppredict\data_qwen_split)，每个样本由两部分组成：

- 页面截图 `images/*.jpg`
- UI 树结构 `jsons/*.json`

同时已提供：

- `train`
- `val`
- `test`
- `manifests/*.jsonl`

### 3.2 类别体系
当前数据包含 22 个场景类别：

- `Ads_Promotion`
- `Dialog_Popup`
- `Error_Blank`
- `Form_Input`
- `Game`
- `Home_Menu`
- `Live Streaming`
- `Login_Signup`
- `Map_Navigation`
- `Messages_Chat`
- `Music Player`
- `News_Article`
- `Other`
- `Payment`
- `Product Details`
- `Product List`
- `Profile_Account`
- `Search`
- `Settings`
- `Shopping Cart`
- `Tutorial`
- `Video Player`

### 3.3 数据特点

- 数据规模足够支撑个人项目完成完整实验闭环。
- 截图与 JSON 一一对应，适合做双分支输入。
- 类别不均衡明显，`Other` 类较大，`Payment`、`Shopping Cart` 等类别较少。
- 当前标签是人工场景类，适合作为第一阶段监督信号。

## 4. 产品目标

### 4.1 总目标
构建一个基于“截图 + UI 树 JSON”的轻量多模态移动界面语义系统，实现分类、检索和聚类三类核心能力，并形成可答辩、可演示、可复现实验结果。

### 4.2 分阶段目标

- 第一阶段：完成 22 类页面场景分类基线。
- 第二阶段：学习统一语义向量，支持相似页面检索。
- 第三阶段：在语义空间中进行聚类，构建场景原型库。

### 4.3 成功指标

- 分类任务：Top-1 Accuracy、Macro-F1 达到稳定可用水平。
- 检索任务：同类页面在 Recall@K 上优于图像单模态基线。
- 聚类任务：聚类结果具备可解释性，能形成有意义的场景簇。
- 工程任务：完成一套可重复运行的数据读取、训练、评估和检索分析流程。

## 5. 本期范围

### 5.1 必做范围

- 读取 `data_qwen_split` 中的截图与 JSON 数据。
- 训练图像单模态分类基线。
- 训练截图 + UI 树的轻量双分支分类模型。
- 输出 embedding，并完成相似页面检索。
- 对 embedding 做聚类并生成原型页面。

### 5.2 可选增强

- 引入度量学习损失，如 Triplet Loss 或 SupCon Loss。
- 对 `Other` 类进行子簇分析。
- 做简单演示系统，如输入页面返回类别和相似页面。

### 5.3 暂不纳入本期

- 复杂端侧部署
- 在线智能体闭环
- OCR 扩展
- 多步任务自动执行
- 超大模型蒸馏系统

## 6. 核心用户与价值

### 6.1 目标用户

- 移动界面理解研究者
- 自动化测试与智能体方向学生
- 希望研究跨应用界面语义的本科团队

### 6.2 核心价值

- 将“截图分类”升级为“多模态场景语义建模”
- 为后续跨应用检索、自动化与推荐提供统一表示
- 形成个人大创中少见但又可落地的完整技术链路

## 7. 产品方案

## 7.1 总体思路
整体采用“先监督分类、再语义表示、最后检索聚类”的轻量路线：

1. 用 22 类标签训练稳定分类模型
2. 从模型中提取语义向量 embedding
3. 用 embedding 做相似页面检索
4. 用 embedding 做聚类并生成原型页

这一路线的优点是：

- 每个阶段都能单独出实验结果
- 能充分利用现有标签和配对数据
- 比直接做开放集聚类更稳
- 更符合大创时间和个人实现能力

## 7.2 输入设计

### 7.2.1 图像输入
页面截图作为视觉输入，负责提供布局、配色、图标、自绘控件和整体风格信息。

### 7.2.2 UI 树输入
JSON 文件中包含控件层级、文本、坐标、可点击性、可滚动性、资源标识等信息。项目中可先将其线性化为节点序列，再输入轻量结构编码器。

### 7.2.3 多模态配对方式
同一个 `sample_id` 下的 `jpg + json` 组成一条训练样本，标签由其所在类别目录决定。

## 7.3 模型设计

### 7.3.1 图像基线模型
先使用轻量图像模型建立基线，建议优先选择：

- `MobileNetV3`
- `ResNet18`

其作用是先验证截图单模态的上限。

### 7.3.2 多模态主模型
采用双分支轻量架构：

- 图像分支：编码页面截图
- UI 树分支：编码 JSON 线性化后的节点序列
- 融合层：将两路特征拼接或门控融合
- 分类头：输出 22 类场景结果

这个架构足够轻量，且实现难度明显低于复杂的图神经网络或大型多模态 Transformer。

### 7.3.3 语义表示输出
在分类头之前保留一层固定维度 embedding，作为页面语义向量，用于：

- 相似页面检索
- 聚类分析
- 原型页面选择

## 7.4 训练策略

### 7.4.1 第一阶段：分类训练
目标是先获得稳定的分类能力。

训练重点：

- 处理类别不均衡
- 关注 `Other` 类混淆
- 对比图像单模态和多模态模型效果

### 7.4.2 第二阶段：表示学习增强
在分类损失基础上，可引入简单度量学习方法增强 embedding 区分性，例如：

- Triplet Loss
- Contrastive Loss

如果时间不足，这一阶段也可以先用分类模型倒数第二层特征直接作为 embedding。

### 7.4.3 第三阶段：检索与聚类
完成训练后，对全量页面提取 embedding，并开展：

- Top-K 相似页面检索
- t-SNE / UMAP 可视化
- K-Means / HDBSCAN 聚类
- 每簇原型页选择

## 8. 功能需求

### 8.1 数据读取模块

- 支持读取 `jpg + json + label`
- 支持读取 `manifest`
- 支持 train/val/test 三个 split

### 8.2 预处理模块

- 图像 resize 与标准化
- JSON 节点提取与线性化
- 文本清洗、字段筛选和坐标归一化

### 8.3 训练模块

- 图像单模态训练
- 多模态分类训练
- checkpoint 保存与日志输出

### 8.4 评估模块

- 分类指标统计
- 混淆矩阵
- 检索 Recall@K
- 聚类质量分析

### 8.5 分析展示模块

- 单样本预测
- 相似页面展示
- 场景簇与原型页展示

## 9. 数据模型设计

### 9.1 单条样本
每条样本至少包含：

- `sample_id`
- `label`
- `split`
- `image_path`
- `json_path`

### 9.2 JSON 结构关注字段
训练中优先使用以下字段：

- `bounds`
- `content-desc`
- `resource-id`
- `clickable`
- `scrollable-horizontal`
- `scrollable-vertical`
- `children`
- 节点类型与祖先类型

### 9.3 标签设计
当前以 22 类标签作为主监督信号，不额外引入复杂多层标签体系，保证实现简洁。

## 10. 评估体系

### 10.1 分类评估

- Accuracy
- Precision / Recall / F1
- Macro-F1
- 混淆矩阵

### 10.2 检索评估

- Recall@1
- Recall@5
- Recall@10

### 10.3 聚类评估

- NMI
- ARI
- Silhouette Score

### 10.4 对比实验
建议至少做三组对比：

- 图像单模态
- UI 树单模态
- 图像 + UI 树多模态

这样能直接体现多模态输入是否真正有效。

## 11. 项目实施计划

### M1：数据理解与基线搭建

- 跑通 `data_qwen_split` 数据读取
- 完成图像分类基线训练
- 输出基础分类结果

### M2：UI 树编码与多模态融合

- 完成 JSON 线性化
- 搭建双分支模型
- 完成多模态分类实验

### M3：检索与聚类

- 提取 embedding
- 完成相似页面检索
- 完成场景聚类和原型页分析

### M4：总结与答辩材料

- 总结模型效果与失败案例
- 输出可视化结果
- 形成开题、中期或结题材料

## 12. 项目风险与应对

### 12.1 类别不均衡
应对：使用类别权重、重采样或 Focal Loss，并关注 Macro-F1。

### 12.2 JSON 信息冗余
应对：只保留核心字段，先做轻量线性化，不做过复杂图结构建模。

### 12.3 多模态模型过重
应对：坚持轻量双分支，不引入过大的 Transformer。

### 12.4 项目范围失控
应对：以“分类、检索、聚类”三件事为主，不把智能体执行和端侧部署纳入本期刚需。

## 13. 创新点与特色

### 13.1 基于真实配对数据的多模态界面理解
不同于只做截图分类，本项目使用截图与 UI 树配对数据，更贴近真实页面语义理解。

### 13.2 面向个人大创的轻量技术路线
不依赖大模型和复杂端侧工程，而是通过可复现的多模态小模型完成核心验证。

### 13.3 从分类到检索再到聚类的完整闭环
不仅完成页面分类，还进一步验证模型学到的语义空间是否具有可解释性和泛化性。

## 14. 预期成果

- 一套可训练的多模态移动界面数据组织方案
- 一个图像分类基线模型
- 一个截图 + UI 树的轻量多模态模型
- 一套页面相似检索与原型聚类实验结果
- 一份适用于大创答辩与结题的完整报告材料

## 15. 推荐项目摘要
本项目面向跨应用移动界面理解问题，基于现有已完成划分的截图与 UI 树 JSON 配对数据，研究一种适合个人大创实现的轻量多模态界面语义建模方法。项目首先构建 22 类页面场景分类基线，在此基础上设计图像分支与 UI 树分支相结合的双分支模型，学习统一的页面语义表示，并进一步实现相似页面检索与场景原型聚类。项目重点解决类别不均衡、结构信息利用不足和界面语义表示能力有限等问题，最终形成一个具备分类、检索和聚类能力的移动界面语义系统，为后续跨应用自动化、智能体感知与界面理解研究提供基础支撑。
